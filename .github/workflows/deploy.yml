name: Deploy to Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to Vercel
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
          VITE_SUPABASE_PROJECT_ID: ${{ secrets.VITE_SUPABASE_PROJECT_ID }}

      - name: Deploy to Vercel
        run: |
          echo "🚀 Déploiement automatique sur Vercel"
          echo "Configurez Vercel CLI pour déployer automatiquement"
        # Pour activer le déploiement Vercel :
        # 1. Installez vercel CLI : npm i -g vercel
        # 2. Ajoutez VERCEL_TOKEN dans les secrets GitHub
        # 3. Décommentez les lignes suivantes :
        # - name: Deploy
        #   run: |
        #     npm i -g vercel
        #     vercel --prod --token=${{ secrets.VERCEL_TOKEN }}
